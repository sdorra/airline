import java.io.*;
import org.apache.maven.it.*;

Verifier verifier = new Verifier( basedir.getAbsolutePath() );

String test1 = verifier.getBasedir() + "/target/help/Args1.1";
verifier.assertFilePresent(test1);
verifier.assertFileMatches(test1, "(?s).*args1 description.*");
verifier.assertFileMatches(test1, "(?s).*[.]SH NAME.*");